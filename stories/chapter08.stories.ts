import {
  matrixCalculator,
  textileGenerator,
  textileRepeater
} from '../src/chapter08';

export default { title: 'Chapter08：行列の織りなす模様' };

export const MatrixCalculator = {
  render: matrixCalculator
};

export const TextileGenerator = {
  render: textileGenerator
};

export const TextileRepeater = {
  render: textileRepeater
};
